{
  "name": "simple-sync",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "common-tags": "^1.8.0",
    "twilio": "^3.66.0"
  },
  "devDependencies": {
    "twilio-run": "^3.1.1"
  },
  "engines": {
    "node": "12"
  }
}
